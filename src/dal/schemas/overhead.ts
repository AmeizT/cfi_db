/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * CFI Workspace API
 * Backend API for managing church assemblies, members, attendance, finances, and reports for the CFI Workspace platform.
 * OpenAPI spec version: 1.0.0
 */
import * as zod from 'zod';



/**
 * Handles CRUD for Overheads. Supports batch creation (list of overheads)
or single overhead creation. Automatically assigns assembly from user.
 */
export const overheadListResponseAmountRegExp = new RegExp('^-?\\d{0,12}(?:\\.\\d{0,2})?$');


export const OverheadListResponseItem = zod.object({
  "id": zod.number(),
  "amount": zod.stringFormat('decimal', overheadListResponseAmountRegExp).optional(),
  "notes": zod.string().optional(),
  "timestamp": zod.iso.date(),
  "assembly": zod.number(),
  "report": zod.number().nullable(),
  "overhead_type": zod.string(),
  "overhead_type_name": zod.string()
})
export const OverheadListResponse = zod.array(OverheadListResponseItem)

/**
 * Supports batch creation if the payload is a list.
 */
export const overheadCreateBodyAmountRegExp = new RegExp('^-?\\d{0,12}(?:\\.\\d{0,2})?$');


export const OverheadCreateBody = zod.object({
  "amount": zod.stringFormat('decimal', overheadCreateBodyAmountRegExp).optional(),
  "notes": zod.string().optional(),
  "timestamp": zod.iso.date(),
  "overhead_type_name": zod.string()
})

/**
 * Handles CRUD for Overheads. Supports batch creation (list of overheads)
or single overhead creation. Automatically assigns assembly from user.
 */
export const OverheadRetrieveParams = zod.object({
  "id": zod.string()
})

export const overheadRetrieveResponseAmountRegExp = new RegExp('^-?\\d{0,12}(?:\\.\\d{0,2})?$');


export const OverheadRetrieveResponse = zod.object({
  "id": zod.number(),
  "amount": zod.stringFormat('decimal', overheadRetrieveResponseAmountRegExp).optional(),
  "notes": zod.string().optional(),
  "timestamp": zod.iso.date(),
  "assembly": zod.number(),
  "report": zod.number().nullable(),
  "overhead_type": zod.string(),
  "overhead_type_name": zod.string()
})

/**
 * Handles CRUD for Overheads. Supports batch creation (list of overheads)
or single overhead creation. Automatically assigns assembly from user.
 */
export const OverheadUpdateParams = zod.object({
  "id": zod.string()
})

export const overheadUpdateBodyAmountRegExp = new RegExp('^-?\\d{0,12}(?:\\.\\d{0,2})?$');


export const OverheadUpdateBody = zod.object({
  "amount": zod.stringFormat('decimal', overheadUpdateBodyAmountRegExp).optional(),
  "notes": zod.string().optional(),
  "timestamp": zod.iso.date(),
  "overhead_type_name": zod.string()
})

export const overheadUpdateResponseAmountRegExp = new RegExp('^-?\\d{0,12}(?:\\.\\d{0,2})?$');


export const OverheadUpdateResponse = zod.object({
  "id": zod.number(),
  "amount": zod.stringFormat('decimal', overheadUpdateResponseAmountRegExp).optional(),
  "notes": zod.string().optional(),
  "timestamp": zod.iso.date(),
  "assembly": zod.number(),
  "report": zod.number().nullable(),
  "overhead_type": zod.string(),
  "overhead_type_name": zod.string()
})

/**
 * Handles CRUD for Overheads. Supports batch creation (list of overheads)
or single overhead creation. Automatically assigns assembly from user.
 */
export const OverheadPartialUpdateParams = zod.object({
  "id": zod.string()
})

export const overheadPartialUpdateBodyAmountRegExp = new RegExp('^-?\\d{0,12}(?:\\.\\d{0,2})?$');


export const OverheadPartialUpdateBody = zod.object({
  "amount": zod.stringFormat('decimal', overheadPartialUpdateBodyAmountRegExp).optional(),
  "notes": zod.string().optional(),
  "timestamp": zod.iso.date().optional(),
  "overhead_type_name": zod.string().optional()
})

export const overheadPartialUpdateResponseAmountRegExp = new RegExp('^-?\\d{0,12}(?:\\.\\d{0,2})?$');


export const OverheadPartialUpdateResponse = zod.object({
  "id": zod.number(),
  "amount": zod.stringFormat('decimal', overheadPartialUpdateResponseAmountRegExp).optional(),
  "notes": zod.string().optional(),
  "timestamp": zod.iso.date(),
  "assembly": zod.number(),
  "report": zod.number().nullable(),
  "overhead_type": zod.string(),
  "overhead_type_name": zod.string()
})

/**
 * Handles CRUD for Overheads. Supports batch creation (list of overheads)
or single overhead creation. Automatically assigns assembly from user.
 */
export const OverheadDestroyParams = zod.object({
  "id": zod.string()
})

